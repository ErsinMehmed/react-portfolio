// Raw hex for JS consumers that can't take a Tailwind class (e.g. the QR
// code's fgColor prop). Mirrors the `brand` color in tailwind.config.js —
// keep both in sync if the brand color changes.
export const BRAND = "#1b74e4";
